---

machine:
  node:
    version: "node"

checkout:
  post:
    - git submodule sync
    - git submodule update --init --recursive

dependencies:
  override:
    - yarn
  post:
    - yarn run bootstrap && yarn run build

test:
  override:
    - ./smoke/circleci.sh:
        parallel: true
